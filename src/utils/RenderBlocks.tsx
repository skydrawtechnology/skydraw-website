import { blocksList } from './blocksList'

export const RenderBlocks = ({ layouts }: any) => {
    return (
        <div>
            {/* {layouts.map((layout: any, ind: number) => { */}
            {/*     const Block = blocksList[layout?.blockType] */}
            {/*     if (blocksList[layout?.blockType]) { */}
            {/*         return <Block layout={layout} key={ind} /> */}
            {/*     } else { */}
            {/*         return <h1>Hello world</h1> */}
            {/*     } */}
            {/* })} */}
        </div>
    )
}
